namespace Compiler_1.Services
{
    public enum TokenType
    {
        Keyword,
        Identifier,
        Punctuation,
        Whitespace,   // пробелы, табуляции, переводы строк
        Error
    }
}